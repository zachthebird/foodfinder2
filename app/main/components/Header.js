import React from "react";

var Header = (props) => {
    return(
        <div className="jumbotron text-center">
            <h1>Welcome to FoodFinder</h1>
            <p>Having difficulty deciding on a meal? Step right up.</p>           
        </div>
    );
}

export default Header;